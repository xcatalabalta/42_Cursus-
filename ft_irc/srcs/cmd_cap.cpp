#include "Parser.class.hpp"


void Parser::_cmd_cap(Client * client)
{
    // std::stringstream msg;
    // std::cout << "CAP detectado con estos parametros" << std::endl;
    // msg << "CAP detectado con estos parametros" << std::endl;
    // for (size_t i = 1; i < this->_tokens.size(); ++i) {
    //     msg << this->_tokens[i] << std::endl;
    //     std::cout << this->_tokens[i] << std::endl;
    // }
	// client->enqueueIrcMessage(msg.str());
    // msg.str("");
    // msg.clear();
    if (client == NULL) {return ;}
    return ;
}
