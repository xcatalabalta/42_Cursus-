#include "Parser.class.hpp"
void Parser::_cmd_pong(Client * client)
{
    (void)client;
    // std::stringstream msg;
    // std::cout << "PONG detectado con estos parametros" << std::endl;
    // msg << "PONG detectado con estos parametros" << std::endl;
    // for (size_t i = 1; i < this->_tokens.size(); ++i) {
    //     msg << this->_tokens[i] << std::endl;
    //     std::cout << this->_tokens[i] << std::endl;
    // }
	// client->enqueueIrcMessage("PING :" + this->_tokens[1]);
    // return ;
}
